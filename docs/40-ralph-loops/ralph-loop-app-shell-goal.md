# App Shell Ralph Loop Goal

Use this as the Codex `/goal` text for the next loop:

```text
Implement the `apps/web` app shell integration from `docs/40-ralph-loops/ralph-loop-app-shell-prompt.md`, `docs/20-specs/app-shell-spec.md`, `docs/30-testing/testing-app.md`, `docs/10-architecture/package-boundaries.md`, `docs/20-specs/design-tokens-spec.md`, and `docs/20-specs/kit-spec.md`.

Preserve the implemented core, domain, design-token, Tailwind, UI, kit, and Storybook packages.

Create the first real Next.js app surface only: homepage, `/deals/northstar-energy` deal route rendering `@repo/kit/DealDashboardDemo`, app-level translations, font/theme wiring, route loading/error/not-found UI, and Playwright e2e coverage.

Do not implement the commitment form, React Hook Form, tRPC, GraphQL, auth, database code, server actions, route handlers, API clients, or live data fetching.

Maintain `PLAN.md` and `STATUS.md`. Stop after full verification and report results.
```
