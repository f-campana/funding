# Ralph Loop Goal - Kit Package

Implement `packages/kit` from `docs/40-ralph-loops/ralph-loop-kit-prompt.md`,
`docs/20-specs/kit-spec.md`, `docs/30-testing/testing-kit.md`, `docs/10-architecture/package-boundaries.md`,
`docs/20-specs/domain-spec.md`, `docs/20-specs/ui-spec.md`, and `docs/50-research/funding.md`.

Preserve the implemented core, domain, design-token, Tailwind, and UI packages.
Create product-shaped composed React components, stories, tests, and contract
checks only. Do not implement app routes, data fetching, tRPC, auth, or the
React Hook Form commitment form. Stop after full verification and report
results.
